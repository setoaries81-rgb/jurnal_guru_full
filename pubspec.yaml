name: jurnal_guru
description: Aplikasi Jurnal Guru (Guru & Admin) - Flutter (template with attendance + CSV export)
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  intl: ^0.19.0
  path_provider: ^2.1.4
  share_plus: ^10.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/sample_teacher.jpg
